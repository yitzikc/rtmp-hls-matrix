{
  "name": "rtmp-hls-matrix",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "npx rimraf ./build",
    "build": "npm run clean && npx tsc",
    "start": "npm run build && node build/index.js",
    "start:dev": "nodemon build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/restify": "^8.4.2",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "node-ansible": "^0.5.5",
    "restify": "^8.5.1"
  }
}
